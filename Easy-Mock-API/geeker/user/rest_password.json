{
  "code": function({
    _req,
    Mock
  }) {
    let body = _req.body;
    if (typeof body.id == 'string') {
      return 200
    } else {
      return 500
    }
  },
  "msg": function({
    _req,
    Mock
  }) {
    let body = _req.body;
    if (typeof body.id == 'string') {
      return "重置用户密码成功"
    } else {
      return '重置用户密码失败'
    }
  },
}